/** Common Mobilize group / event type labels (free text also allowed in API). */
export const MOBILIZE_GROUP_TYPES = ["reading", "prayer", "march", "support", "meeting", "other"] as const;

export const MOBILIZE_EVENT_TYPES = ["march", "prayer", "meeting", "vigil", "training", "other"] as const;
