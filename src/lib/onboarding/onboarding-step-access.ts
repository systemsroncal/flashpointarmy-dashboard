/** Navigation to Mission Briefing is enabled; progress rules apply in onboarding-navigation and page guards. */
export function isMissionBriefingNavigationEnabled(): boolean {
  return true;
}

/** Navigation to Choose Your First Mission is enabled; progress rules apply in onboarding-navigation and page guards. */
export function isFirstMissionNavigationEnabled(): boolean {
  return true;
}
