/** Temporarily hide navigation to Mission Briefing (keep labels + info tooltips visible). */
export function isMissionBriefingNavigationEnabled(): boolean {
  return false;
}

/** Temporarily hide navigation to Choose Your First Mission (keep labels + info tooltips visible). */
export function isFirstMissionNavigationEnabled(): boolean {
  return false;
}
