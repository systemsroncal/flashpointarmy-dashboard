/**
 * @deprecated Unused — the dashboard uses a unified sidebar in `DashboardShell`
 * (`ChaptersGroupsNavGroup`, Feed, Profile, Deployment, Mobilize settings).
 * Kept as a stub so old imports fail loudly at typecheck if reintroduced incorrectly.
 */
export function MobilizeSidebarNav(_props: {
  onNavigate?: () => void;
  showSettings?: boolean;
}): null {
  return null;
}
