/**
 * Auth pages (login, register, forgot password) — assets under /public.
 */
export const LOGIN_BACKGROUND_IMAGE = "/backgrounds/login-register-bg.jpg";

/** Mark next to “F.P. ARMY” on the left column. */
export const LOGIN_LOGO_LEFT_MARK = "/logos/fp-army-logo.svg";

/**
 * Rounded square icon in the form header (book/crosshair) — next to DreamsTrack / F.P. ARMY.
 */
export const LOGIN_LOGO_FORM_ICON = "/logos/fp-army-logo.svg";

/** Optional wide lockup; form header uses FORM_ICON + text by default. */
export const LOGIN_LOGO_FORM_MARK = "/logos/DreamsTrack-fp-army-logo.svg";

/** Large watermark (top right). */
export const LOGIN_LOGO_WATERMARK = "/logos/flash-point-logo.svg";

/** Dashboard left drawer header (`DashboardShell`). */
export const DASHBOARD_DRAWER_LOGO = "/logos/Dashboard-Logo.svg";
